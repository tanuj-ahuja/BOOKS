
<% if logged_in? %>

<div class="microposts">
<h3>BOOK!</h3>
<%= render @micropost %>
</div>
<% end %>
<% unless current_user?(@user) %>
  
   <% if @items==[] %>
  <div>
    <%= form_for(current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :seller_id, @micropost.user_id %></div>
  <div><%= hidden_field_tag :message_id, @micropost.id %></div>
  <%= f.submit "Send Request", class: "btn btn-primary" %>
  <% end %>
  <% else %>
   <h3>
   	 Request already sent......
   </h3>
  <% end %>

<% end %>
  </div>